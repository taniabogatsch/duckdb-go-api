GENERATOR:
  PackageName: duckdb
  PackageDescription: "Package duckdb-go-api provides Go bindings for DuckDB's C extension API"
  PackageLicense: "THE AUTOGENERATED LICENSE. ALL THE RIGHTS ARE RESERVED BY ROBOTS."
  Options:
    SafeStrings: true

PARSER:
  SourcesPaths: ["go_duckdb.h", "duckdb_extension.h", "magic.h"]

TRANSLATOR:
  ConstCharIsString: true
  ConstUCharIsString: false
  ConstRules:
    defines: cgo
  Rules:
    global:
      - {transform: lower}
      - {action: accept, from: "duckdb_"}
      - {action: replace, from: "duckdb_"}
      - {transform: export}
    type:
      - { action: replace, from: "_t$" }
    private:
      - {transform: unexport}
    post-global:
      - {action: replace, from: _$}
      - {load: snakecase}